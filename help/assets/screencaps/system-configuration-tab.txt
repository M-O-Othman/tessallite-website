Screenshot of the System Admin page with the "Configuration" tab selected. The right panel shows a search box at the top, then collapsible accordion sections (Authentication, Scheduling, Gateway timeouts, Rate limiting, XMLA, Optimizer, Result limits, Schema drift, LLM, Frontend) each holding rows of settings. Each row has the dotted key on the left, the current value in the middle (text input or switch), and Reset / Save buttons on the right. A small orange restart icon appears next to settings flagged restart_required. At the very bottom, a "Bootstrap (env-only)" section lists read-only env values with secrets masked as "***** (32 chars)". Dimensions: 1280x900px.
